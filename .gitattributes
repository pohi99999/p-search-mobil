# Normalise line endings so a Windows-side edit stops showing every file as
# modified. Text is stored with LF in the repository and checked out with LF.
* text=auto eol=lf

# Binary files present in this repository -- never touch their bytes.
*.png -text
