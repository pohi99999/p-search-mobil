Conductor-Task: Phase 6 - Jules Async Integration Round 4 (ai_context typing, secure storage, n8n webhook cleanup, BillingContext & TesterProgress fixes)

Conductor-Summary: Cleanly merged 5 remote async branches from Jules into master using the 'ort' strategy. 1) Integrated actionplan ai_context type tightening (database.ts and ActionPlanScreen.tsx). 2) Removed hardcoded fallback N8N webhook URL (constants.ts) and added checks for undefined webhook url in HomeScreen.tsx and OnboardingScreen.tsx to avoid silent fetch failures. 3) Migrated Supabase session storage on native devices from AsyncStorage to expo-secure-store for hardened security (package.json, app.json, supabase.ts). 4) Removed local hardcoded mock fallback data from TesterProgress.tsx, migrating to centralized logger.error and updated its test suite for 0-day failure behavior. 5) Integrated unit tests for BillingContext error path (restorePurchases error handling and loading states). Verified the integration with npm install --legacy-peer-deps (Success), npx tsc --noEmit (Passed with 0 errors), and npx jest (Passed: 9/9 suites, 36/36 tests). No temporary session folders were found or left behind.
