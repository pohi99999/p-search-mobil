Conductor-Task: Phase 6 - Jules Async Integration (OCR fallback banners, Snackbar dedup, cleanup)
Conductor-Summary: Integrated Jules' async session output from jules_session_11143774421442787145/ into ActionPlanScreen.tsx and PaywallScreen.tsx: added a low-confidence OCR fallback Banner (react-native-paper) and ocrConfidence/uploadError/uploading state to both screens. PaywallScreen.tsx required cleanup: the patch script (patch_screens.sh, sed-based) had left 5 duplicated <Snackbar> blocks and omitted the Banner/Snackbar imports, causing unresolved-name compile errors; consolidated to a single Snackbar bound to uploadError and added the missing imports. Note: the session artifact only contained the two screen files (no migration or useActionPlan.test.ts were present, contrary to the task description) so no DB trigger or new test file was integrated in this pass. Removed the temporary jules_session_11143774421442787145/ directory after integration (it was polluting npx tsc --noEmit scope with unresolved relative imports). Verified npx tsc --noEmit (0 errors) and npx jest (5/5 passing, existing documentGenerator.test.ts suite only).
