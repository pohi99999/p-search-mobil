# React Native Style Guide

- Use Functional Components and React Hooks exclusively.
- Use `react-native-safe-area-context` to handle notches and safe areas.
- Keep components small and reusable.
- Use `react-native-paper` for all standard UI components to ensure a consistent Material Design look.
- Extract complex logic into custom hooks.
